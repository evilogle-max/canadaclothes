{
  "name": "canadaclothes",
  "version": "1.0.0",
  "private": true,
  "description": "Minimalist apparel e-commerce with Vercel + Supabase",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "start": "vercel start",
    "test": "jest",
    "test:e2e": "playwright test",
    "lint": "eslint src/ && prettier --check src/",
    "lint:fix": "eslint src/ --fix && prettier --write src/"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "jest": "^29.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": "20.x"
  }
}
